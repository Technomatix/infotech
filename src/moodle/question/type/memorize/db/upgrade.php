<?php
defined('MOODLE_INTERNAL') || die();

/**
 * Upgrade code for the memorize question type.
 *
 * @param int $oldversion the version we are upgrading from.
 */
function xmldb_qtype_memorize_upgrade($oldversion) {
    global $CFG, $DB;

    //$dbman = $DB->get_manager();

    return true;
}
